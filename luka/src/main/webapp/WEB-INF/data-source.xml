<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           https://www.springframework.org/schema/context/spring-context.xsd">

    <context:property-placeholder
            location="classpath:application.properties,classpath:application-secret.properties"
            ignore-unresolvable="false"/>

    <bean id="dataSource" class="com.zaxxer.hikari.HikariDataSource" destroy-method="close">
        <property name="driverClassName" value="${snowflake.driver}"/>
        <property name="jdbcUrl" value="${snowflake.url}"/>
        <property name="username" value="${snowflake.user}"/>
        <property name="password" value="${snowflake.password}"/>
        <property name="maximumPoolSize" value="5"/>
        <property name="minimumIdle" value="0"/>
        <property name="connectionTimeout" value="30000"/>
        <property name="connectionTestQuery" value="SELECT CURRENT_VERSION()"/>
    </bean>

    <bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
        <property name="dataSource" ref="dataSource"/>
    </bean>
</beans>
